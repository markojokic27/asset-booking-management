info:
  name: User
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/v1/reports/users/:id"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
