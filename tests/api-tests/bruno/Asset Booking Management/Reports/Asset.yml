info:
  name: Asset
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/v1/reports/assets/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
